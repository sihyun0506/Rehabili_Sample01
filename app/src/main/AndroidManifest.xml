<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rehabili_sample1">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_rehabili"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_rehabili_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Rehabili_sample1">
        <activity android:name=".ui.arm.elbow.ElbowSet"></activity>
        <activity android:name=".ui.arm.elbow.ElbowFinish" />
        <activity android:name=".ui.arm.elbow.ElbowCounting" />
        <activity android:name=".ui.arm.elbow.ElbowActivity" />
        <activity android:name=".ui.arm.ArmActivity" /> <!-- loading screen -->
        <activity
            android:name="com.example.rehabili_sample1.ui.first_loading.LoadingActivity"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
            android:screenOrientation="portrait">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter> <!-- main -->
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">

        </activity>
    </application>

</manifest>